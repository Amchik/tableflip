.cache/
bin/
obj/

# Test table. For table examples see example/ folder
/test

# Generate new compile_commands.json via bear:
# `TARGET=DEBUG bear -- make` (or `...bear make`)
/compile_commands.json

